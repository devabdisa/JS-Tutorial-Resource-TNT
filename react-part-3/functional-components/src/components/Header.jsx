const Header = () => {
    return (
        <header className="header">
            <div className="logo">ReactFund.</div>
            <nav className="nav-links">
                <a href="#">Home</a>
                <a href="#">Concepts</a>
                <a href="#">Contact</a>
            </nav>
        </header>
    );
};

export default Header;
